package main

/*
一般来说，刚安装好go之后需要配置gopath.

gopath配置为工作目录，例如：E:/workspace
GOROOT配置为go的安装目录，例如：D:/go
GOBIN配置为gopath/bin，会将安装的可执行文件放在这个文件夹下，如果不配置，默认是GOROOT/bin

go1.17之后，go get只用来下载依赖，即普通的包，如果安装可执行程序，应该使用 go install
*/
